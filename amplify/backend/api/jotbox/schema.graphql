type Gaps @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String!
  order: Int
}

type Node @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  description: String
  gaps: [ID]
  pined: Boolean!
  archived: Boolean!
  trashed: Boolean!
  collaborators: [AWSEmail!]
  reminderTime: AWSDateTime
  reminderInterval: AWSTime
  order: Int
  images: [String]
  color: String
}
 