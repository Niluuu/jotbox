type Gaps @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String!
}

type Node @model @auth(rules: [{allow: public}]) {
  id: ID!
  title: String
  description: String
  gaps: [ID]
  pined: Boolean!
  archived: Boolean!
}
 